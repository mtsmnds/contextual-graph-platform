---
name: dev-docs
description: DEPRECATED — use dev-workflow instead
license: MIT
compatibility: opencode
---

> **DEPRECATED: This skill is superseded by `dev-workflow`.**
>
> Load `dev-workflow` instead:
> ```
> skill({ name: "dev-workflow" })
> ```
>
> **Mode mapping:**
> - `devdocs init` → unchanged in dev-workflow
> - `update` → renamed to `update` mode in dev-workflow
> - `archive-plan` → folded into `prd end` mode in dev-workflow
>
> The templates in this directory are still used by `dev-workflow`'s `devdocs init` mode.

